# Dependencies & build output (reinstalled / rebuilt inside the image)
node_modules/
.next/
out/

# Secrets (NEXT_PUBLIC_API_URL is passed as a build arg instead)
.env
.env.*

# VCS / editor / OS noise
.git/
.gitignore
npm-debug.log*
yarn-debug.log*
*.log
.DS_Store
.idea/
.vscode/
coverage/
