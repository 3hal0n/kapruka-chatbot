# Dependencies & build output (reinstalled / rebuilt inside the image)
node_modules/
.next/
out/

# Secrets (BACKEND_ORIGIN and Clerk keys are passed via docker-compose environment/args instead)
.env
.env.*

# VCS / editor / OS noise
.git/
.gitignore
npm-debug.log*
yarn-debug.log*
*.log
.DS_Store
.idea/
.vscode/
coverage/
